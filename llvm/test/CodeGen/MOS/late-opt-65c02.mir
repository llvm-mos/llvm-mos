# NOTE: Assertions have been autogenerated by utils/update_mir_test_checks.py
# RUN: llc -mtriple=mos -mcpu=mos65c02 -run-pass=mos-late-opt -verify-machineinstrs -o - %s | FileCheck %s
---
name: ldayimm_to_inaydeay
body: |
  bb.0.entry:
    ; CHECK-LABEL: name: ldayimm_to_inaydeay
    ; CHECK: $a = LDImm 0
    ; CHECK-NEXT: $y = LDImm 5
    ; CHECK-NEXT: $a = INC $a
    ; CHECK-NEXT: $y = INC $y
    ; CHECK-NEXT: $a = DEC killed $a
    ; CHECK-NEXT: $y = DEC killed $y
    ; CHECK-NEXT: RTS implicit $a, implicit $y
    dead $a = LDImm 0
    dead $y = LDImm 5
    $a = LDImm 1
    $y = LDImm 6
    $a = LDImm 0
    $y = LDImm 5
    RTS implicit $a, implicit $y
...
