# NOTE: Assertions have been autogenerated by utils/update_mir_test_checks.py
# RUN: llc -mtriple=mos -run-pass=prologepilog -verify-machineinstrs -o - %s | FileCheck %s -check-prefixes=CHECK,6502
# RUN: llc -mtriple=mos -mcpu=mos65c02 -run-pass=prologepilog -verify-machineinstrs -o - %s | FileCheck %s -check-prefixes=CHECK,65C02
--- |
  target datalayout = "e-p:16:8:8-i16:8:8-i32:8:8-i64:8:8-f32:8:8-f64:8:8-a:8:8-Fi8-n8"
  target triple = "mos"

  define void @norecurse() norecurse {
  entry:
    ret void
  }

  define void @norecurse_variable_sized() norecurse {
  entry:
    ret void
  }

  define void @interrupt_axy() "interrupt" {
  entry:
    ret void
  }

  define void @interrupt_x() "interrupt" {
  entry:
    ret void
  }

  define void @interrupt_csr() "interrupt" {
  entry:
    ret void
  }

  define void @interrupt_csrs_save_y() "interrupt" {
  entry:
    ret void
  }

  define void @interrupt_caller() "interrupt" {
  entry:
    ret void
  }
---
name: norecurse
stack:
  - { id: 0, size: 1}
  - { id: 1, size: 1}
body:             |
  bb.0.entry:
    ; CHECK-LABEL: name: norecurse
    ; CHECK: $c = LDCImm 0
    ; CHECK: PH $a
    ; CHECK: $a = COPY $rc0
    ; CHECK: $a, $c, $v = ADCImm $a, 254, $c
    ; CHECK: PH killed $a
    ; CHECK: $a = COPY $rc1
    ; CHECK: $a, $c, $v = ADCImm $a, 255, $c
    ; CHECK: $rc1 = COPY killed $a
    ; CHECK: $a = PL
    ; CHECK: $rc0 = COPY killed $a
    ; CHECK: $a = PL
    ; CHECK: KILL killed $p
    ; CHECK: STAbs $a, target-index(mos-static-stack) :: (store 1 into %stack.0)
    ; CHECK: $a = LDAbs target-index(mos-static-stack) + 1 :: (load 1 from %stack.1)
    ; CHECK: $c = LDCImm 0
    ; CHECK: $a = COPY $rc0
    ; CHECK: $a, $c, $v = ADCImm $a, 2, $c
    ; CHECK: $rc0 = COPY killed $a
    ; CHECK: $a = COPY $rc1
    ; CHECK: $a, $c, $v = ADCImm $a, 0, $c
    ; CHECK: $rc1 = COPY killed $a
    ; CHECK: RTS
    STAbsOffset $a, %stack.0, 0 :: (store 1 into %stack.0)
    $a = LDAbsOffset %stack.1, 0 :: (load 1 from %stack.1)
    ADJCALLSTACKDOWN 1, 0, implicit-def $rs0, implicit $rs0
    ADJCALLSTACKUP 1, 0, implicit-def $rs0, implicit $rs0
    ADJCALLSTACKDOWN 2, 0, implicit-def $rs0, implicit $rs0
    ADJCALLSTACKUP 2, 0, implicit-def $rs0, implicit $rs0
    RTS
...
---
name: norecurse_variable_sized
stack:
  - { id: 0, type: variable-sized }
  - { id: 1, size: 1 }
  - { id: 2, size: 2 }
body: |
  bb.0.entry:
    ; CHECK-LABEL: name: norecurse_variable_sized
    ; CHECK: $a = frame-setup COPY $rc6
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup COPY $rc7
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $rs3 = COPY $rs0
    ; CHECK: $a = LDAbs target-index(mos-static-stack) + 2 :: (load 1 from %stack.2 + 1)
    ; CHECK: $rs0 = COPY $rs3
    ; CHECK: $a = frame-destroy PL
    ; CHECK: $rc7 = frame-destroy COPY killed $a
    ; CHECK: $a = frame-destroy PL
    ; CHECK: $rc6 = frame-destroy COPY killed $a
    ; CHECK: RTS implicit $rc6, implicit $rc7
    $a = LDAbsOffset %stack.2, 1 :: (load 1 from %stack.2 + 1)
    RTS
...
---
name: interrupt_axy
body: |
  bb.0.entry:
    ; CHECK-LABEL: name: interrupt_axy
    ;  6502: frame-setup PH $a
    ;  6502: $a = frame-setup COPY $x
    ;  6502: frame-setup PH killed $a
    ;  6502: $a = frame-setup COPY $y
    ;  6502: frame-setup PH killed $a
    ; 65C02: frame-setup PH $a
    ; 65C02: frame-setup PH $x
    ; 65C02: frame-setup PH $y
    ; CHECK: $a = frame-setup LDAbs &__save_a
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup LDAbs &__save_y
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $x = LDImm 42
    ; CHECK: $a = LDImm 43
    ; CHECK: $y = LDImm 44
    ; CHECK: $a = frame-destroy PL
    ; CHECK: frame-destroy STAbs killed $a, &__save_y
    ; CHECK: $a = frame-destroy PL
    ; CHECK: frame-destroy STAbs killed $a, &__save_a
    ;  6502: $a = frame-destroy PL
    ;  6502: $y = frame-destroy COPY killed $a
    ;  6502: $a = frame-destroy PL
    ;  6502: $x = frame-destroy COPY killed $a
    ;  6502: $a = frame-destroy PL
    ; 65C02: $y = frame-destroy PL
    ; 65C02: $x = frame-destroy PL
    ; 65C02: $a = frame-destroy PL
    ; CHECK: RTI implicit $a, implicit $x, implicit $y
    $x = LDImm 42
    $a = LDImm 43
    $y = LDImm 44
    RTI
...
---
name: interrupt_x
tracksRegLiveness: true
body: |
  ; CHECK-LABEL: name: interrupt_x
  ; CHECK: bb.0.entry:
  ; CHECK:   successors: %bb.1(0x80000000)
  ; CHECK:   liveins: $a, $x
  ;  6502:   frame-setup PH $a
  ;  6502:   $a = frame-setup COPY $x
  ;  6502:   frame-setup PH killed $a
  ; 65C02:   frame-setup PH $a
  ; 65C02:   frame-setup PH $x
  ; CHECK:   $a = frame-setup LDAbs &__save_a
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup LDAbs &__save_y
  ; CHECK:   frame-setup PH killed $a
  ; CHECK: bb.1.entry:
  ; CHECK:   $x = LDImm 42
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   frame-destroy STAbs killed $a, &__save_y
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   frame-destroy STAbs killed $a, &__save_a
  ;  6502:   $a = frame-destroy PL
  ;  6502:   $x = frame-destroy COPY killed $a
  ;  6502:   $a = frame-destroy PL
  ; 65C02:   $x = frame-destroy PL
  ; 65C02:   $a = frame-destroy PL
  ; CHECK:   RTI implicit $a, implicit $x
  bb.0.entry:
  bb.1.entry:
    $x = LDImm 42
    RTI
...
---
name: interrupt_csr
tracksRegLiveness: true
body: |
  ; CHECK-LABEL: name: interrupt_csr
  ; CHECK: bb.0.entry:
  ; CHECK:   successors: %bb.1(0x80000000)
  ; CHECK:   liveins: $a, $rc6, $rc7
  ; CHECK:   frame-setup PH $a
  ; CHECK:   $a = frame-setup COPY $rc6
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup COPY $rc7
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup LDAbs &__save_a
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup LDAbs &__save_y
  ; CHECK:   frame-setup PH killed $a
  ; CHECK: bb.1.entry:
  ; CHECK:   $rs3 = IMPLICIT_DEF
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   frame-destroy STAbs killed $a, &__save_y
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   frame-destroy STAbs killed $a, &__save_a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   $rc7 = frame-destroy COPY killed $a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   $rc6 = frame-destroy COPY killed $a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   RTI implicit $a, implicit $rc6, implicit $rc7
  bb.0.entry:
  bb.1.entry:
    $rs3 = IMPLICIT_DEF
    RTI
...
---
name: interrupt_csrs_save_y
tracksRegLiveness: true
body: |
  ; CHECK-LABEL: name: interrupt_csrs_save_y
  ; CHECK: bb.0.entry:
  ; CHECK:   successors: %bb.1(0x80000000)
  ; CHECK:   liveins: $a, $y, $rc6, $rc7, $rc8, $rc9, $rc10, $rc11
  ; CHECK:   $c = LDCImm 0
  ; CHECK:   PH $a
  ; CHECK:   $a = COPY $rc0
  ; CHECK:   $a, $c, $v = ADCImm $a, 254, $c
  ; CHECK:   PH killed $a
  ; CHECK:   $a = COPY $rc1
  ; CHECK:   $a, $c, $v = ADCImm $a, 255, $c
  ; CHECK:   $rc1 = COPY killed $a
  ; CHECK:   $a = PL
  ; CHECK:   $rc0 = COPY killed $a
  ; CHECK:   $a = PL
  ; CHECK:   KILL killed $p
  ; CHECK:   frame-setup PH $a
  ;  6502:   $a = frame-setup COPY $y
  ;  6502:   frame-setup PH killed $a
  ; 65C02:   frame-setup PH $y
  ; CHECK:   $a = frame-setup COPY $rc6
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup COPY $rc7
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup COPY $rc8
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup COPY $rc9
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup LDAbs &__save_a
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = frame-setup LDAbs &__save_y
  ; CHECK:   frame-setup PH killed $a
  ; CHECK:   $a = COPY $rc10
  ; CHECK:   $y = LDImm 0
  ; CHECK:   STYIndir killed $a, $rs0, killed $y :: (store 1 into %stack.1)
  ; CHECK:   $a = COPY $rc11
  ; CHECK:   $y = LDImm 1
  ; CHECK:   STYIndir killed $a, $rs0, killed $y :: (store 1 into %stack.0)
  ; CHECK: bb.1.entry:
  ; CHECK:   $rs3 = IMPLICIT_DEF
  ; CHECK:   $rs4 = IMPLICIT_DEF
  ; CHECK:   $rs5 = IMPLICIT_DEF
  ; CHECK:   $y = LDImm 1
  ; CHECK:   $a = LDYIndir $rs0, killed $y :: (load 1 from %stack.0)
  ; CHECK:   $rc11 = COPY killed $a
  ; CHECK:   $y = LDImm 0
  ; CHECK:   $a = LDYIndir $rs0, killed $y :: (load 1 from %stack.1)
  ; CHECK:   $rc10 = COPY killed $a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   frame-destroy STAbs killed $a, &__save_y
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   frame-destroy STAbs killed $a, &__save_a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   $rc9 = frame-destroy COPY killed $a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   $rc8 = frame-destroy COPY killed $a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   $rc7 = frame-destroy COPY killed $a
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   $rc6 = frame-destroy COPY killed $a
  ;  6502:   $a = frame-destroy PL
  ;  6502:   $y = frame-destroy COPY killed $a
  ; 65C02:   $y = frame-destroy PL
  ; CHECK:   $a = frame-destroy PL
  ; CHECK:   PH $a
  ; CHECK:   $c = LDCImm 0
  ; CHECK:   $a = COPY $rc0
  ; CHECK:   $a, $c, $v = ADCImm $a, 2, $c
  ; CHECK:   $rc0 = COPY killed $a
  ; CHECK:   $a = COPY $rc1
  ; CHECK:   $a, $c, $v = ADCImm $a, 0, $c
  ; CHECK:   $rc1 = COPY killed $a
  ; CHECK:   $a = PL
  ; CHECK:   RTI implicit $a, implicit $y, implicit $rc6, implicit $rc7, implicit $rc8, implicit $rc9, implicit $rc10, implicit $rc11
  bb.0.entry:
  bb.1.entry:
    $rs3 = IMPLICIT_DEF
    $rs4 = IMPLICIT_DEF
    $rs5 = IMPLICIT_DEF
    RTI
...
---
name: interrupt_caller
body: |
  bb.0.entry:
    ; CHECK-LABEL: name: interrupt_caller
    ;  6502: frame-setup PH $a
    ;  6502: $a = frame-setup COPY $x
    ;  6502: frame-setup PH killed $a
    ;  6502: $a = frame-setup COPY $y
    ;  6502: frame-setup PH killed $a
    ; 65C02: frame-setup PH $a
    ; 65C02: frame-setup PH $x
    ; 65C02: frame-setup PH $y
    ; CHECK: $a = frame-setup COPY $rc2
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup COPY $rc3
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup COPY $rc4
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup COPY $rc5
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup LDAbs &__save_a
    ; CHECK: frame-setup PH killed $a
    ; CHECK: $a = frame-setup LDAbs &__save_y
    ; CHECK: frame-setup PH killed $a
    ; CHECK: JSR &fn, mos_csr
    ; CHECK: $a = frame-destroy PL
    ; CHECK: frame-destroy STAbs killed $a, &__save_y
    ; CHECK: $a = frame-destroy PL
    ; CHECK: frame-destroy STAbs killed $a, &__save_a
    ; CHECK: $a = frame-destroy PL
    ; CHECK: $rc5 = frame-destroy COPY killed $a
    ; CHECK: $a = frame-destroy PL
    ; CHECK: $rc4 = frame-destroy COPY killed $a
    ; CHECK: $a = frame-destroy PL
    ; CHECK: $rc3 = frame-destroy COPY killed $a
    ; CHECK: $a = frame-destroy PL
    ; CHECK: $rc2 = frame-destroy COPY killed $a
    ;  6502: $a = frame-destroy PL
    ;  6502: $y = frame-destroy COPY killed $a
    ;  6502: $a = frame-destroy PL
    ;  6502: $x = frame-destroy COPY killed $a
    ;  6502: $a = frame-destroy PL
    ; 65C02: $y = frame-destroy PL
    ; 65C02: $x = frame-destroy PL
    ; 65C02: $a = frame-destroy PL
    ; CHECK: RTI implicit $a, implicit $x, implicit $y, implicit $rc2, implicit $rc3, implicit $rc4, implicit $rc5
    ADJCALLSTACKDOWN 0, 0, implicit $rs0, implicit-def $rs0
    JSR &fn, mos_csr
    ADJCALLSTACKUP 0, 0, implicit $rs0, implicit-def $rs0
    RTI
...
